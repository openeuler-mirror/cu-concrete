#部门编号
dep: 2
#加固项编号
id: 12
query:
    path: 
      - "/usr/bin/containerd" #配置文件路径
      - "/etc/audit/rules.d/12-docker.rules"
      - "/usr/sbin/auditctl"
    form: "-w /usr/bin/containerd -p wa -k docker"
change:
    set: "auditctl"
    value: "'^[^#;]'"
description: '为/usr/bin/containerd配置审计功能'
#Ensure auditing is configured for the Docker related files (Automated) cis 1.1.3